# SUMMY · Устав (L0) — как мы работаем

> **Канон всей экосистемы SUMMY.** Читают ВСЕ участники (модели/агенты) в начале работы — во всех продуктах.
> Это решение уровня экосистемы и процесса разработки, поэтому оно живёт **здесь**, а не внутри одного продукта.
>
> **Правила базы знаний:** один факт — один источник (канон здесь; `CLAUDE.md`/`AGENTS.md` продуктов — короткие памятки + указатель сюда); живое состояние не дублируем прозой (версии, «что на проде» — спрашиваем у системы; git-теги = журнал релизов); меняется только через ревью **архитектора-исполнителя**.
>
> ⚠️ **Облачные сессии видят только свой репозиторий** и до этого файла не дотянутся. Поэтому каждый продукт обязан нести короткую памятку в своём `AGENTS.md` — шаблон: [`templates/AGENTS-stub.md`](templates/AGENTS-stub.md).

## Продукты экосистемы

Четыре продукта + платформа данных. Архитектор — **один на все**: ценность и риск живут на **стыках** между ними.

| Продукт | Что это | Репозиторий |
|---|---|---|
| **Приложение мастера** | Кабинет мастера (Django), `master.summy.ru` | `kirillsummy/SUMMY` |
| **Приложение клиента** | Клиентское приложение — **пока не существует** | `clientapp` (план) |
| **Админ / операционная платформа** | Дашборд управляющих и админов, `admin.summy.ru` | `kirillsummy/adminapp` |
| **Сайт** | `summy.ru` | `summymanager-rgb/summy` |
| **Платформа данных** | API-шлюз + наша БД (raw/core/contract, [ADR-0001](decisions/0001-data-platform.md)) — общая опора всех продуктов | `summy-data` (план) |

Соседи: `summy-data-gateway` (зеркало данных YClients + БД) — план.

## Что где живёт (границы продуктов)

> **Правило:** прежде чем строить — **определи, чей это продукт**. Строишь не в своём — **остановись и скажи владельцу/архитектору**. Не тащи чужой функционал в свой репозиторий только потому, что ты в нём сидишь.

| Продукт | Что тут строим | Чего тут НЕ строим |
|---|---|---|
| **Приложение мастера** (кабинет, Django) | Всё про мастера: смены, записи дня, его финансы и тариф, профиль, портфолио, его настройки | Управленку, отчёты для студии, админ-экраны |
| **Админ / операционная платформа** (adminapp) | **Вся управленка**: дашборд управляющего, операционные процессы (ремонт/рекламация/обход/стерилизация), отчёты и аналитика студии, генератор QR, процесс «Доработки». **Сюда переезжает то, что жило в Excel** | Экраны мастера |
| **Сайт** | Публичное: лендинги, SEO, блог, вакансии | Личные кабинеты |
| **Платформа данных** (шлюз+БД) | Слои raw/core/contract, синк YClients, обогащения; **пишет только шлюз** | Интерфейсы и продуктовую логику — продукты читают contract-слой |

_Заметка владельца 19.07.2026: «приложение админа + управляющего» со временем может разделиться на два продукта — пока один._
| **Приложение клиента** (пока нет) | Клиентское: запись, история, лояльность | — |

_Грабли 16.07: сессию попросили перенести управленку из Excel; она сидела в репо кабинета, увидела Django — и начала строить управленку **там**. Правильный дом — adminapp (другой стек, своя БД). Сессия видит только свой репозиторий, поэтому «чей это продукт» должно быть написано, а не подразумеваться._

## Межпродуктовые контракты (живут ЗДЕСЬ)

**Контракт, который читают ДВА и более продукта, лежит в этом репозитории — не внутри одного из них.** Причина та же, что у устава: **облачная сессия видит только свой репозиторий**, поэтому контракт внутри чужого продукта для неё не существует.

| Контракт | Кто пишет / кто читает |
|---|---|
| [`contracts/qr-workplace.md`](contracts/qr-workplace.md) | Генератор QR — админ-платформа; читатель — кабинет мастера |

_Внутренние контракты продукта (модель оплаты кабинета, опции услуг) остаются в его репо — их читает один продукт._

## Роли

- **Владелец / архитектор системы — Кирилл (человек).** Направление, продукт, приоритеты, ресурсы, «что и зачем». Принимает продуктовые развилки. Назначает резервного релиз-инженера, когда основной недоступен.
- **Архитектор-исполнитель — Claude Code.** Превращает замысел в структуру; ревью, версии, мерж в `dev`, теги, выкатка; **держит стыки между продуктами**; координирует агентов; отвечает за качество и процесс. **Один на всю экосистему** (не по одному на продукт) и **дефолт-держатель эстафеты релиза**.
- **Рабочая сессия / субагент.** Спавнится под задачу, живёт коротко, **отдаёт результат архитектору-исполнителю**. НЕ мержит в `dev`/`main`, НЕ ставит теги, НЕ деплоит.
- **Воркеры (эксплуатация).** Периодические (синк, отчёты, health-check). **Предлагают, а не исполняют необратимое.** Любой сторонний эффект — только через гейт архитектора-исполнителя.

_Почему архитектор один: локальный архитектор «только своего продукта» не видит стык. Пример: QR рабочих мест физически лежит в БД кабинета, а место ему в админ-платформе — такое решение принимает только тот, кто держит оба конца. Реализация при этом параллелится по продуктам (рабочие сессии), голова по стыкам — одна._

## Релиз-процесс (стандарт)

1. **Источник правды — `dev`.** Релизы — аннотированные теги `vX.Y.Z`. Прод = задеплоенный тег. **Ничего на проде мимо `dev`.**
2. **Прод сам сообщает версию.** `VERSION` пишется при деплое (тег + sha + дата + кто) и отдаётся health-эндпоинтом. «Что на проде» = *спросить прод*, а не гадать.
3. **Журнал релизов = git-теги + `CHANGELOG`** (Keep a Changelog: `[Unreleased]` копится → переносится в версию при выкатке).
4. **Один релиз-инженер в моменте.** Дефолт — Claude. Кончились лимиты — владелец **явно** передаёт эстафету. **Двое одновременно не выкатывают** (иначе гонка тегов — уже ловили).
5. **Протокол «догона»** (в начале релиз-смены / при возврате): спросить версию прода → сверить с тегами и `dev` → проверить миграции → только потом мержить/выкатывать.

## Лестница замещения YClients (утверждено владельцем 23.07.2026)

Когда продукту не хватает данных и рука тянется позвать YClients напрямую —
порядок строго такой (принцип Ильи Байкова, дополнен архитектором):

1. **Данные ТЕКУТ в зеркало.** Не «таблица есть в БД», а: объект покрыт синком
   шлюза, строки наполняются, свежесть измерима маячком. Таблица без потока —
   пустышка; недостающую таблицу добавляет alembic-ревизия шлюза, недостающий
   синк — его импортёр.
2. **Ручка шлюза существует И её свежесть подходит потребителю.** Зеркало
   отстаёт на минуты: ведомости этого не заметят, экран реального времени —
   заметит. Ручка «есть» — мало; заявленная свежесть класса данных обязана
   покрывать нужду конкретного экрана.
3. **Замена — поэтапно и обратимо**: за фича-флагом или с фолбэком, включение —
   отдельным решением после маячка (образец — сетевая карточка клиента).

**Граница лестницы — только ЧТЕНИЕ.** Запись в YClients (создание записей,
правка графиков, авторизация мастеров) НЕ замещается зеркалом: YClients —
действующая система записи, и «замена» записи молча оторвёт студию от неё.
Любое замещение записи — отдельное продуктовое решение владельца, не шаг агента.

Новые читатели данных не начинают с прямых вызовов YClients вовсе: если данные
уже в зеркале — сразу ручка шлюза (одна дверь).

## Параллельные прод-сессии (утверждено владельцем 23.07.2026)

Владелец может вести инфраструктурные работы (переезды серверов, миграции
компонентов) отдельной сессией параллельно с релиз-инженером. Это норма, а не
нарушение. Опасен не параллелизм, а молчаливый параллелизм — каждое правило
ниже выросло из реального инцидента:

1. **Перед переключением боевого трафика — сигнал релиз-инженеру** (одна
   строка: «переключаю X на сервер Y»). В окно переключения релизы не
   выкатываются. *(23.07: выкат adminapp лёг на старый сервер за минуты до
   переключения — живой прод молча остался на прошлой версии.)*
2. **После переезда — сверка версий.** Каждый компонент по своему
   health-эндпоинту показывает ту же версию, что до переезда; расхождение —
   это потерянный выкат, чинится сразу. Инструмент — `prod-status` в корне
   рабочего пространства: одна команда, все компоненты.
3. **Скрипты выката и доки перенацеливаются в том же заходе**, что и переезд, —
   не «потом». Иначе следующий релиз уезжает на мёртвый сервер с «зелёным»
   локальным логом. *(23.07: deploy-prod.sh кабинета целился в резерв.)*
4. **Схема боевой БД меняется только через канонический журнал миграций**
   своего репозитория. Смена самого механизма журнала (раннера) — только
   через `dev` и явное решение владельца; применять раннер из невлитой ветки
   к бою нельзя. *(13.07: две миграции-«листа» уронили кабинет в 502;
   23.07: журнал платформы данных подменён из невлитой ветки.)*

## Ветки · версии · миграции

- `feature/<…>` от `dev`; после мержа ветку удалять. Версии **монотонны, не переиспользовать** (SemVer; pre-1.0 → `0.y.z`).
- **Заморозка после хендоффа:** отданную ветку не дописывать; готовые коммиты не переписывать — `amend`, `rebase`, `push --force` **запрещены** (история append-only). Новая доработка = новая ветка/итерация.
- Миграции доходят до прода **только через `dev`**. Конфликт миграций → **merge-нода**, НЕ ручная хирургия боевой БД.

## Жёсткие запреты (для всех, кроме релиз-инженера в его окне)

- Не выкатывать в прод мимо процесса: ssh/docker/deploy-скрипты на боевых серверах.
- Не пушить и не мержить в `main`/`dev`, не ставить теги — это делает только релиз-инженер.
- Не удалять данные/файлы/миграции и не делать необратимое без явного указания владельца.
- Не создавать аккаунты, не менять права/секреты, не подключать платные сервисы, не отправлять наружу.

## Хендофф архитектору (как рабочая сессия заканчивает)

Проверки зелёные (сборка/линт/типы + тесты; красное = не отдавать) → коммит в **свою** ветку → пуш **ветки** (не `dev`/`main`) → запись в `CHANGELOG` `[Unreleased]` → сводка «сделано / проверить / осталось» → **последняя строка**:

```
Архитектору: смержи и выкати <ветка> (commit <полный SHA>)
```

Не готово к проду → `Архитектору: в прод не мержить, ветка в работе`.

_В продуктах, где принят PR-поток (напр. админ-платформа): финал = **Pull Request** + та же строка архитектору. Мерж/деплой — не сессия._

## Что документируем, а что нет (контракт — вперёд, а не всё — вперёд)

Тест один: **«зависит ли от этого кто-то снаружи — другой модуль, другой агент, другой экран?»**

- **Да, пересекает границу → документ = истина**, пишем/обновляем **вместе с кодом** (в том же коммите/PR): границы модулей, схема данных для читающих, контракты API/интеграций, доменные правила. Крупную/рискованную фичу предваряет короткая записка (что и зачем), не полный спек.
- **Нет, живёт внутри одной фичи и ещё лепится → без жёсткого документа.** Истина о поведении = **код + тесты**, не проза. Не документируем каждую функцию (протухнет и станет врать).
- **Переезд.** Застыло и на это обопёрся кто-то ещё → стало контрактом → переезжает в документ.

_«Истина — код» для поведения остаётся в силе; документ — истина для **намерения и контракта**, а не повод ломать рабочий код под устаревший абзац._

## База знаний — слои (растим по нужде, не впрок)

- **L0** — этот устав (роли + процесс + запреты). Общий для экосистемы.
- **L1** — карта архитектуры (границы модулей, где что живёт, источник правды).
- **L2** — домен (модель оплаты, грейды, услуги/опции, YClients).
- **L3** — конвенции (код-стиль, бренд, версии, миграции).
- **L4** — ранбуки (деплой, догон, инцидент) — для воркеров.

_Принцип: не плодить документы впрок (устареют), а консолидировать существующее и добавлять слои по мере реальной нужды._

## Бренд

Имя — **SUMMY** (не «SUMMY Beauty»). Тон — тёплый, на «ты», без восклицаний и давления.
